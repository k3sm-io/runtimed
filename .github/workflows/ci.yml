# WORKFLOWS DISABLED 2026-07-20 — operator directive (see workspace docs/BACKLOG.md run log).
# Every line below is commented out; uncomment to re-enable.
# # INTERIM M7-bootstrap public CI for runtimed.
# #
# # Runs the repo's hack/ci.sh gate (gofmt / boilerplate / vet / build / test /
# # mod-tidy — CGO=1, owned by the script), a -race pass, and the SPI
# # symbol-canary on every PR. This is a bootstrap gate: the richer M7.2
# # cross-repo gate subsumes it later.
# #
# # Security posture mirrors dco.yml: pull_request trigger (NEVER
# # pull_request_target), read-only token, GitHub-hosted ephemeral runner,
# # third-party actions pinned by commit SHA.
# #
# # runtimed is CGO=1 and its go.mod pins `replace k3sm.io/apis => ../apis`; apis
# # publishes no tags, so a lone checkout cannot build. We check out apis as a
# # sibling under $GITHUB_WORKSPACE so the relative replace resolves on disk (no
# # go.work needed). Siblings are checked out at their default branch (main) —
# # acceptable for this workflow-only change.
# name: CI
# on:
#   # CI DISABLED — no auto-run. macOS runners are billable and we are not spending
#   # on CI for now (operator directive 2026-07-02). Manual-only via workflow_dispatch;
#   # re-enable by restoring `pull_request:`.
#   workflow_dispatch:
# permissions:
#   contents: read
# concurrency:
#   group: ci-${{ github.ref }}
#   cancel-in-progress: true
# jobs:
#   ci:
#     runs-on: macos-15
#     timeout-minutes: 20
#     steps:
#       - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
#         with:
#           path: runtimed
#       - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
#         with:
#           repository: k3sm-io/apis
#           path: apis
#       - uses: actions/setup-go@3041bf56c941b39c61721a86cd11f3bb1338122a # v5.2.0
#         with:
#           go-version-file: runtimed/go.mod
#       - name: gate
#         working-directory: runtimed
#         run: hack/ci.sh
#       - name: race
#         working-directory: runtimed
#         run: CGO_ENABLED=1 go test -race ./...
#       - name: spicanary
#         working-directory: runtimed
#         run: CGO_ENABLED=1 go test ./internal/spicanary/...
